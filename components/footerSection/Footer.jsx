"use client";

import styles from './FooterSection.module.css';

export default function Footer() {
    return (
        <footer className={`${styles.classCall} bg-dark`}>
            <h1 className='bg-primary'>Footer Section</h1>
        </footer>
    )
}