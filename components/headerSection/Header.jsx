"use client";

import styles from './HeaderSection.module.css';

export default function Header () {
    return (
        <header>
            <h1 className={styles.classCall}>Header</h1>
        </header>
    )
}